export const handleAddUserToQueue = (patientInformation: any): number => {
  console.log('Should be added to QUEUE', patientInformation);
  // Criteria should be to give the user with less pain less priority.
  return 0;
};
